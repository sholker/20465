gcp_compute_instance:
    name: "{{ item }}"
    machine_type: n1-standard-1
    ... # any other settings
    zone: us-central1-a
    project: "astral-genre-316321"
    auth_kind: "service_account_file"
    service_account_file: "~/.gcp.json"
    state: present
loop:
  - instance-1
  - instance-2